# BMI 지수를 계산하여 정상여부를 출력합니다.
# 키와 몸무게를 차례대로 입력 받습니다.
# 키는 미터 단위입니다.

# BMI = (몸무게) / (키 * 키)
# 35 <= BMI : 고도비만
# 30 <= BMI < 35 : 중도비만
# 25 <= BMI < 30 : 경도비만
# 23 <= BMI < 25 : 과체중
# 18.5 <= BMI < 23 : 정상
# BMI < 18.5 : 저체중

# 키와 몸무게를 입력받기
height = float(input("키(m)를 입력하세요: "))
weight = float(input("몸무게(kg)를 입력하세요: "))

# BMI 계산
bmi = ??

# BMI 지수에 따라 비만 여부 출력
if bmi >= 35:
    print()
elif bmi >= 30:
    print()
elif ??
    print()
elif bmi >= 23:
    print()
elif ??
    print()
else:
    print()
